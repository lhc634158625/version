<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="config">
    <select id="GetBaseDictById" parameterType="int" resultType="teamwish.dataContracts.base.BaseDict">
        select id,dictName,code,name,parameter from BaseDict where id = #{id}
    </select>
<select id="GetBaseDictByFilter" parameterType="teamwish.dataContracts.common.PageFilter" resultType="teamwish.dataContracts.base.BaseDict"  statementType="STATEMENT">
        SELECT id,dictName,code,name,parameter FROM (select ROW_NUMBER() Over(order by ${orderField} ) as rowNum, * from BaseDict  where ${filter}) tb where rowNum between ${fromRecord} and ${toRecord}
    </select>
 <insert useGeneratedKeys="true" keyProperty="id" id="InsertBaseDict" parameterType="teamwish.dataContracts.base.BaseDict">
        insert into BaseDict(dictName,code,name,parameter) values(#{dictName},#{code},#{name},#{parameter})
    </insert>
 <delete id="DeleteBaseDict" parameterType="int">
        delete from BaseDict where id=#{id}
    </delete>
 <update id="UpdateBaseDict" parameterType="teamwish.dataContracts.base.BaseDict">
        update BaseDict set 
   dictName=#{dictName},
   code=#{code},
   name=#{name},
   parameter=#{parameter}
  where id=#{id}
    </update>


</mapper>

